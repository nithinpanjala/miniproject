package com.stg.service;
import com.stg.entity.Cart;
import com.stg.entity.Dish;
public interface CartServices {
	/*
	 * public Cart addDish(int cartNo ,Dish dish,int quantity); public Cart
	 * removeDish(int cartNo ,Dish dish); public Cart updateDishQuantity(int cartNo
	 * ,Dish dish , int quantity); public Float getTotalPrice(int cartNo ,Cart
	 * cart); public Float getTotalPriceAfterDiscount(int cartNo ,Float discount);
	 * public Cart PlaceOrder(int cartNo ,Cart cart);
	 */

}
